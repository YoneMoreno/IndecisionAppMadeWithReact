.big-button {
  background: $purple;
  border: none;
  border-bottom: .6rem solid darken($purple, 15%);
  color: white;
  font-weight: bold;
  font-size: $l-size;
  margin-bottom: $xl-size;
  padding: $m-size;
  width: 100%;
}

.big-button:disabled {
  opacity: .5;
}